package com.healthfamily.web.dto;

import lombok.Data;

/**
 * InferenceGenerateRequest
 * <p>
 * 承载平台业务功能，供上层调用或作为领域组件使用。
 * </p>
 */
@Data
public class InferenceGenerateRequest {
    // 可以添加如 startDate, endDate 等参数，目前为空
}
